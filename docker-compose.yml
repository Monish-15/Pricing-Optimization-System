version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexus-pricing-app
    ports:
      - "8000:8000"
    volumes:
      - nexus-data:/app/backend

volumes:
  nexus-data:
